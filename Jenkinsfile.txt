pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Use 'bat' for Windows batch commands instead of 'sh'
                bat 'echo Hello World'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Include any test scripts here, e.g., calling batch or test commands
                bat 'echo Testing...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the project...'
                // Deployment steps if applicable
                bat 'echo Deployment step'
            }
        }
    }
    post {
        always {
            echo 'Cleaning up workspace...'
            deleteDir()
        }
        failure {
            echo 'Build failed!'
        }
        success {
            echo 'Build succeeded!'
        }
    }
}
